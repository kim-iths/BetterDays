import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 12,
    },
    infoContainer: {
        marginTop: 8,
        flexDirection: "row",
        alignItems:"center"

    },
    nameText: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 4,
    },
    infoText: {
        marginLeft:4,
    },

})

export default styles